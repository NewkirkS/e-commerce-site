
import { productByCategory } from 'board-game/helpers/product-by-category';
import { module, test } from 'qunit';

module('Unit | Helper | product by category');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = productByCategory([42]);
  assert.ok(result);
});

